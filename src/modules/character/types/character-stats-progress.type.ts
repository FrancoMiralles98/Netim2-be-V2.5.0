export interface CharacterStatsProgress {
    base_hp: number;
    base_regen_hp: number;
    base_VIT: number;
    base_DEX: number;
    base_INT: number;
    base_STR: number;
    ad_per_lv: number;
    ap_per_lv: number;
    hp_per_lv: number;
    va_per_lv: number;
    vm_per_lv: number;
    vh_per_lv: number;
}