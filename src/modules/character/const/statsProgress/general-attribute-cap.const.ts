/**
 * @description
 * El cap general de cualquier atributo, cada raza tiene su propio cap
 * que esta limitado para los `puntos_atributo`, pero este se puede superar
 * mediante bonus de items, buff, etc, hasta un maximo que lo define este valor
 */
export const GENERAL_ATTRIBUTE_CAP = 200