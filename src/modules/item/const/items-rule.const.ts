export const EQUIP_RULES = {
  MAX_CORRUPT_EXPLICIT_BONUS: 3,
  MAX_EXPLICIT_BONUS: 5,
  MAX_6_7_BONUS: 2,
} as const;

export const PIEDRA_ROTA = {
  idItem: 0,
};

export const MONUTRA_RULES = {
  HEALING_PER_ITEM: 0.1, //por cada vez que alimentas le curas como max un 10% de su HP MAX
};
